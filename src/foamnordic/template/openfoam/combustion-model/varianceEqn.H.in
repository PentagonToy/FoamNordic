#error "FoamNordic combustion scaffold: supply the solver's variance equation"

// Variance convention and dimensions are part of the native contract. The
// concrete solver must enforce 0 <= variance <= mean*(1-mean) when progress is
// normalized to [0, 1], or document another admissible domain.

@ASSEMBLE_PROGRESS_VARIABLE_VARIANCE_EQUATION@
@RELAX_CONSTRAIN_AND_SOLVE_VARIANCE_EQUATION@
@BOUND_VARIANCE_USING_THE_DECLARED_POLICY@
